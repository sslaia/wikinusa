name: game_crosswords
description: A reusable Crossword puzzle module for Nias language games.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  easy_localization: ^3.0.8
  shared_preferences: ^2.5.5
  share_plus: ^13.1.0
  path_provider: ^2.1.5
  http: ^1.6.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
